# איך עובדים עם גיט בפרויקט הזה

**חשוב לפני הכל: הריפו חייב להיות Private.** החוברת אוסרת במפורש לשתף קוד ברשת
(עבירת משמעת), אז לא ריפו ציבורי בשום שלב, גם לא אחרי ההגשה.

התיקייה הזאת כבר מאותחלת כריפו גיט עם קומיט ראשון על ענף main,
אז נשארו רק שלושה צעדים.

## שלב א - ליה מעלה לגיטהאב (פעם אחת)
1. נכנסים ל-github.com, לוחצים New repository.
   שם: `mmn14-assembler`, בוחרים **Private**, בלי README ובלי gitignore
   (יש לנו כבר), ולוחצים Create repository.
2. פותחים טרמינל בתוך התיקייה `assembler` ומריצים:
   ```
   git remote add origin https://github.com/liasamuelov/mmn14-assembler.git
   git push -u origin main
   ```
   אם גיט מבקש הזדהות: מאשרים דרך הדפדפן, או יוצרים Personal Access Token
   בהגדרות גיטהאב ומדביקים אותו במקום סיסמה.
3. מוסיפים את מאיה: בריפו בגיטהאב, Settings > Collaborators > Add people.

## שלב ב - מאיה מורידה (פעם אחת)
```
git clone https://github.com/liasamuelov/mmn14-assembler.git
cd mmn14-assembler
make
```

## שלב ג - שגרת העבודה היומית (שתיכן)
- לפני שמתחילות לעבוד: `git pull`
- אחרי שמסיימות קטע עבודה:
  ```
  git add -A
  git commit -m "תיאור קצר של מה שעשיתי"
  git push
  ```
- כל אחת עובדת רק על הקבצים שלה (רשום בראש כל קובץ), אז כמעט לא יהיו
  קונפליקטים. אם בכל זאת קופץ קונפליקט ב-pull: פותחים את הקובץ, בוחרים את
  הגרסה הנכונה בין הסימונים `<<<<<<<` ו-`>>>>>>>`, ואז commit ו-push רגיל.
- קבצי `.o`, קובץ ההרצה וקובצי הפלט לא עולים לגיט - זה כבר מוגדר ב-`.gitignore`.

## בדיקה שהכל תקין
```
make
./assembler tests/ps.as
```
אמור להתקמפל בלי שום אזהרה וליצור את `tests/ps.am`.
