# ההסכמות בינינו (זום, 6.8)

מסמך קצר שמתעד את ההחלטות, כדי שלא נתווכח עליהן שוב בשבוע הבא.

## 1. פענוח שורות - פעם אחת, ב-utils
כל פירוק של שורה (תווית, שם פעולה, אופרנדים) נעשה אך ורק דרך הפונקציות
המשותפות שמוגדרות בסוף `utils.h`:
`extract_label`, `is_legal_label`, `split_operands`, `parse_register`,
`parse_number`. מאיה מממשת אותן ב-`utils.c` (המעבר הראשון צריך אותן
ראשון), ליה משתמשת בהן במעבר השני ובקדם-אסמבלר. אף אחת לא כותבת
פירוק מקומי משלה בתוך מעבר.

## 2. איך המעבר השני יודע מה להשלים
המעבר השני קורא שוב את קובץ ה-am מההתחלה, מדלג על הערות/ריקות/הנחיות
(חוץ מ-entry), ומקדם מונה כתובת ב-4 על כל שורת הוראה, בדיוק כמו המעבר
הראשון. הוא מפענח מחדש (עם הפונקציות מסעיף 1) רק הוראות שתלויות בסמל:
הסתעפויות מותנות והוראות J עם תווית, ומתקן את המילה עם
`image_set_instruction`. אין מבנה "רשימת תיקונים" בין המעברים.

## 3. קידוד - רק דרך encode
בניית מילת 32 הסיביות נעשית אך ורק דרך `build_r_word` / `build_i_word` /
`build_j_word` שבמודול `encode` (מוכן). המעבר הראשון קורא להן עם 0 בשדות
שעוד לא ידועים, והמעבר השני קורא להן שוב עם הערכים הסופיים ודורס את המילה.

## 4. חלוקת השגיאות
- מאיה (מעבר ראשון): כל שגיאה שנראית משורה בודדת - פעולה לא קיימת,
  מספר אופרנדים שגוי, רגיסטר לא חוקי, פסיקים, מספר מחוץ לתחום, תווית
  לא חוקית או מוגדרת פעמיים, שורה ארוכה מ-80.
- ליה (מעבר שני): כל שגיאה שדורשת את טבלת הסמלים - סמל לא מוגדר,
  הסתעפות מותנית לסמל חיצוני, entry לסמל שלא קיים.
- כל ההודעות באנגלית, דרך `report_error`, ולא עוצרים אחרי השגיאה הראשונה.

## 5. חוזה קובץ ה-am
ליה מתחייבת: בקובץ ה-am אין שורות mcro/mcroend ואין שמות מאקרו, כל שאר
השורות מועתקות כלשונן ובסדרן. מאיה מתחייבת: המעבר הראשון מניח שהמאקרואים
כבר נפרשו ולא מטפל בהם.

## 6. מפגשי מיזוג
- 8.8 בערב: מריצים יחד קדם-אסמבלר + מעבר ראשון על `tests/ps.as` ומשווים
  את טבלת הסמלים לטבלה שבחוברת.
- 10.8: מריצים הכל, משווים את `ps.ob` בית-בית מול הדוגמה הפתורה בחוברת.
הדוגמה הפתורה בחוברת היא טסט רשמי בחינם - היא ההגדרה של "עובד".
